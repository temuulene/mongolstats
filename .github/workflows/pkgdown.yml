name: pkgdown

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2
      - uses: r-lib/actions/setup-r@v2

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: r-lib/pkgdown, any::roxygen2
          needs: website

      - name: Generate Rd docs and build site
        env:
          PKGDOWN_EVAL: true
        run: |
          Rscript -e "roxygen2::roxygenise(); pkgdown::build_site(preview = FALSE, new_process = FALSE)"

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: docs
